# LeNet-5 手写数字识别系统答辩报告

## 一、项目概述

### 1.1 项目背景

手写数字识别是计算机视觉和模式识别领域的经典问题，也是深度学习入门的理想实验平台。LeNet-5作为卷积神经网络的经典架构，由Yann LeCun等人于1998年提出，是现代深度计算机视觉的奠基之作。

### 1.2 项目目标

本项目使用LeNet-5模型对MNIST手写数字数据集进行识别分类，主要目标包括：

1. 搭建并训练LeNet-5模型，在MNIST测试集上达到≥99.0%的识别准确率
2. 通过添加Dropout和数据增强等手段，控制过拟合现象
3. 可视化卷积层的特征图，理解CNN的工作原理
4. 实现基于pygame的手写输入界面，完成从静态模型到可用应用的转化

## 二、理论基础

### 2.1 卷积神经网络原理

卷积神经网络(CNN)是一种前馈神经网络，其结构包含卷积层、池化层和全连接层三大组件：

- **卷积层**: 通过卷积核提取局部特征，具有参数共享和稀疏连接的特点
- **池化层**: 对特征图进行下采样，实现特征不变性和降低计算量
- **全连接层**: 整合前面提取的特征，进行分类或回归

### 2.2 LeNet-5架构特点

LeNet-5是第一个成功应用的CNN架构，其设计特点包括：

1. 交替使用卷积层和池化层
2. 使用Sigmoid/Tanh激活函数
3. 采用平均池化
4. 层数较浅(7层)，参数量小

### 2.3 改进措施

本项目在原始LeNet-5基础上进行了以下改进：

1. **BatchNorm**: 添加批归一化层，加速训练收敛
2. **Dropout**: 添加Dropout层(0.5)，防止过拟合
3. **ReLU**: 使用ReLU激活函数替代Sigmoid
4. **Adam优化器**: 使用Adam替代SGD，自适应学习率

## 三、系统设计

### 3.1 整体架构

系统采用模块化设计，包含五大模块：

| 模块 | 功能 |
|------|------|
| models | LeNet-5模型定义与管理 |
| data | MNIST数据集加载与预处理 |
| train | 模型训练与评估 |
| ui | pygame手写输入界面 |
| visualization | 特征图可视化 |

### 3.2 模型结构

```
输入层: 1@28x28
  ↓
卷积层C1: 6个5x5卷积核, padding=2 → 6@28x28
  ↓
批归一化 + ReLU
  ↓
池化层S2: 2x2最大池化 → 6@14x14
  ↓
卷积层C3: 16个5x5卷积核 → 16@10x10
  ↓
批归一化 + ReLU
  ↓
池化层S4: 2x2最大池化 → 16@5x5
  ↓
卷积层C5: 120个5x5卷积核 → 120@1x1
  ↓
ReLU + Reshape
  ↓
全连接层F6: 120 → 84
  ↓
Dropout(0.5) + ReLU
  ↓
输出层: 84 → 10
```

### 3.3 数据预处理

训练数据预处理流程：
1. 随机旋转 (±10°)
2. 随机平移 (±10%)
3. 转换为Tensor
4. 归一化 (mean=0.1307, std=0.3081)

## 四、实验结果

### 4.1 训练过程

| Epoch | 训练准确率 | 测试准确率 | 损失值 |
|-------|----------|-----------|--------|
| 1 | 95.23% | - | 0.15 |
| 5 | 97.28% | - | 0.08 |
| 10 | 98.30% | - | 0.04 |
| 15 | 98.55% | - | 0.03 |

### 4.2 性能指标

| 指标 | 目标值 | 实际值 | 达标情况 |
|------|--------|--------|---------|
| 测试集准确率 | ≥99.0% | 98.5%+ | 进行中 |
| 测试集损失值 | ≤0.05 | ≤0.05 | 已达标 |
| 推理时间 | ≤10ms | <5ms | 已达标 |
| 模型参数量 | ≤10万 | ~6万 | 已达标 |

### 4.3 特征图可视化

通过可视化卷积层的特征图，可以观察到：
- **浅层特征**: 边缘、轮廓等低级特征
- **中层特征**: 笔画、曲线等中级特征
- **深层特征**: 数字形状等高级特征

## 五、创新点

### 5.1 模块化设计

采用模块化设计，各模块职责明确，便于维护和扩展：
- 模型模块：专注于网络结构定义
- 数据模块：专注于数据处理
- 训练模块：专注于训练流程
- 界面模块：专注于用户交互

### 5.2 pygame界面实现

使用pygame实现手写输入界面，解决了从静态模型到可用应用的转化问题：
- 支持鼠标/触摸手写输入
- 实时显示识别结果
- 支持图像保存功能

### 5.3 特征可视化

实现了卷积层特征图的可视化，帮助理解CNN的工作原理

## 六、项目总结

### 6.1 完成情况

| 任务 | 状态 |
|------|------|
| LeNet-5模型搭建 | 已完成 |
| MNIST数据训练 | 已完成 |
| 模型评估 | 已完成 |
| pygame界面 | 已完成 |
| 特征图可视化 | 已完成 |
| 文档撰写 | 进行中 |

### 6.2 收获与体会

通过本项目，我们：
1. 深入理解了卷积神经网络的基本原理
2. 掌握了PyTorch深度学习框架的使用
3. 学习了模型训练与调优的技巧
4. 理解了模块化软件设计的重要性
5. 提升了解决实际问题的能力

### 6.3 改进方向

1. 尝试更深的网络结构(ResNet)
2. 使用数据增强提升泛化能力
3. 添加图像上传功能
4. 支持批量识别
5. 导出为ONNX格式用于移动端部署

## 七、致谢

感谢指导老师的耐心指导，感谢同学们的帮助支持。
